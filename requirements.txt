# Step 1 (benchmark builder) needs only the standard scientific stack:
numpy>=1.24
pandas>=2.0
pyyaml>=6.0

# Step 2 (QLoRA fine-tuning) - install on Kaggle (GPU):
transformers>=4.45
peft>=0.12
bitsandbytes>=0.43
accelerate>=0.33
datasets>=2.20
# torch is preinstalled in the Kaggle GPU image.

# Steps 3-5 (added as we implement them):
# scikit-learn>=1.3   # calibration metrics
